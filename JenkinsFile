pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Hello World!'
//                 bat 'ipconfig'
                bat 'cd'
                sh 'test_file.py'
            }
        }
    }
}
